% =============================================================
% Session 2 -- Part 3 (Second brain) + Part 4 (WIP) — after success stories
% =============================================================

% -------- Part 3 -- Second brain --------
\sectionframe[accent]{Part 3 -- Second brain}

\begin{frame}{Part 3: memory and the Obsidian second brain}
  \twobullets%
    {Session 1 introduced \emph{why} a curated Markdown vault beats blind RAG for research traceability.}%
    {Today: \textbf{how} it works in practice --- my vault layout, project folders, session start/end prompts, and what \emph{not} to store.}%
  \vspace{0.5cm}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{We will show}
      \begin{itemize}\setlength\itemsep{0.35em}
        \item Areas, projects, wiki vs.\ raw notes.
        \item Brain bootstrap: INDEX, CURRENT\_STATE, NEXT\_ACTIONS.
        \item Memanto as semantic layer \emph{on top of} Markdown truth.
        \item Skills and integrations folder.
      \end{itemize}
    \column{0.48\textwidth}
      \textbf{Takeaway for you}
      \begin{itemize}\setlength\itemsep{0.35em}
        \item Start small: one project folder, eight canonical files.
        \item Write decisions in Markdown, not only in chat.
        \item Treat the vault as the restart point every session.
      \end{itemize}
  \end{columns}
\end{frame}

% -------- Part 4 -- Work in progress --------
\sectionframe[accent]{Part 4 -- Work in progress}

\begin{frame}{Part 4: live WIP --- setup, supervision, and fixes}
  \small
  \textbf{1.\ Project bootstrap (live)}
  \begin{itemize}\setlength\itemsep{0.28em}
    \item My standard start: \textbf{Brain} (INDEX, CURRENT\_STATE, wiki) $\rightarrow$ \textbf{GitHub} repo $\rightarrow$ \textbf{Overleaf} sync.
    \item \textbf{Example:} \emph{Mafia migration} --- matched DiD pipeline, analysis plan, wiki, push to GitHub.
  \end{itemize}
  \vspace{0.25cm}
  \textbf{2.\ When the machine needs a human gate}
  \begin{itemize}\setlength\itemsep{0.28em}
    \item \textbf{Purge} --- 1940 INE profession proxies: terrible embedded OCR; rebuilt with image re-OCR, column/row templates, census caps; I spot-checked flagged cells.
    \item Pattern: agent proposes fix $\rightarrow$ I verify against source $\rightarrow$ we lock the auditable pipeline.
  \end{itemize}
  \vspace{0.25cm}
  \textbf{3.\ Interactive contrast}
  \begin{itemize}\setlength\itemsep{0.28em}
    \item Build the same table with \textbf{plain ChatGPT} vs.\ a \textbf{Codex coding agent} on real project files.
  \end{itemize}
  \vspace{0.15cm}
  \begin{center}\srcfoot{Live demos may fail --- that is part of the lesson.}\end{center}
\end{frame}